from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")

from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")

from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")


from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")

from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")


from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")



from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")




from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")




from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")




from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")




from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")




from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")




from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")




from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")




from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")



from itertools import combinations

def main():
	# li = list(map(int, input("Enter numbers with space, we will convert into list for sure.\n").split(" ")))
	# print(f"List: {li}")
	# comb = int(input("Enter comb size"))
	for data in combinations([1,2,3,4,5], 3):
		print(f"My combinations: {data}")

main()
print("Hello Arun !")